# Machine-specific environment variables
# Copy this file to .zsh_env.local and fill in your values
# This file is gitignored - your actual values stay local

# Work organization name (used for ~/dev/<org> directory and git config)
# Re-run bootstrap.sh after setting this to configure git includeIf
export WORK_ORG="your-company"

# Add any other machine-specific variables below
